#include <stdio.h>

int main() {

    float n, soma;

    scanf("%f",&n);
    soma = soma + n;

    scanf("%f",&n);
    soma = soma + n;

    scanf("%f",&n);
    soma = soma + n;

    printf("soma: %.2f", soma);

    
    return 0;
    }